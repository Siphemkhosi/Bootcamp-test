console.log("in greet.js")


    function greet(name){
        if(name = name);{
      return "Hello, " + name ;
    
     
        } ifelse (name = " " || name.length()<=0)

      return "error";
              }
      
      
      var message = greet("bob");
      console.log(message);
      var message = greet ("");
      console.log(message);
    var message = greet("bob");
    console.log(message);